# 2026-04-13

## 今日已完成
- 确认 NoCode 后端本地运行环境：
  - Java：`/home/cabbage/.local/java`
  - Maven：`/home/cabbage/.local/maven`
- 启动并验证后端基础链路可用：注册、登录、登录态、应用创建、应用详情、列表接口
- 验证 HTML 生成接口可进入 SSE 流式返回
- 验证当前主故障：生成文本存在，但代码目录未落盘，预览/下载/部署连锁失败
- 代码层定位：当前保存动作绑定在 streaming complete 之后
- 做过一次手工改造：对 HTML/MULTI_FILE 尝试使用底层 `StreamingChatModel + StreamingChatResponseHandler` 保留真流式并在 `onCompleteResponse` 中保存
- 调查 LangChain4j / DeepSeek streaming 相关公开资料和 issue
- 验证 LangChain4j 版本统一方案不可直接落地（artifact 发布版本不完全对齐）
- 建立文档系统规则和基础目录

## 今日关键结论
- 当前最可信问题方向：DeepSeek + LangChain4j OpenAI-compatible streaming 的收尾/complete 兼容问题
- 当前最适合继续验证的单点：`includeUsage(true)`

## 当前未完成
- 还没有拿到“关闭 includeUsage 后恢复 complete”的验证结果
- 还没有形成最终修复方案
